{
  "name": "makeup-artist-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "nodemailer": "^6.9.15"
  },
  "engines": {
    "node": ">=18"
  }
}
